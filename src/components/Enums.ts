export enum PINTYPE {
  KIRISH = "k",
  CHIQISH = "c",
  NEYTRAL = "n",
}
export enum QUTB {
  MANFIY = -1,
  MUSBAT = 1,
}
export enum POSITION {
  TOP = 8,
  RIGHT = 6,
  BOTTOM = 2,
  LEFT = 4,
}
export enum KUCHLANISH {
  YUQORI = 255,
  PAST = 0,
}
export enum WIRESTATE {
  ON = 1,
  OFF = 0,
}
export enum STYLES {
  AND = "and-gate",
  BORDER = "border",
  FLEX = "flex",
}
export enum TOOLTYPE {
  XOR = "~",
  AND = "&",
  OR = "|",
  NOT = "!",
  NAND = "!&",
  NOR = "!|",
  BATTERY = "b",
  SEG7 = "s7",
  LED = "ld",
  RGBL = "rld",
  BUFFER = "buf",
  TRISTATE = "3st",
  BUTTON = "btn",
  SWITCH = "sw",
}

export enum COLORS {
  BLUE = "007F9E",
  BORDER = "1B4752",
}
