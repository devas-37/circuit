import { Komponent } from './Komponent'
export class Tools {
    rootElement: string = null
    constructor(rootElement: string) {
        this.rootElement = rootElement
    }
    Pins:Array<Komponent>=[]
    Fire(){
        this.Pins.forEach(e=>{
   
        })
    }

}